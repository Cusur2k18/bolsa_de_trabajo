<%= simple_form_for @job_type do |jt| %>
	<%= jt.input :area, label: "Nombre de la carrera" %>
	<%= jt.button :submit, class: 'btn btn-success' %>

	<%= link_to "Regresar", job_types_path, class: ' btn btn-info' %>
<% end %>
